import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles({
  columns: {
    width: '48%',
  },
  smallerText: {
    fontSize: '0.9rem',
  },
  text: {
    marginTop: 30,
    textAlign: 'center',
    width: '100%',
  },
})
